{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col s12">
            <div class="card">
                <div class="card-content">
                    <div class="card-title">
                        RAID {#{{ raid.getRaidTier }} am {{ raid.getDate }}#}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col s12">
            {% include ':raid:listParticipants.html.twig' %}
        </div>
    </div>

{% endblock %}
