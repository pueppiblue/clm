{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col s12">
            <div class="card">
                <div class="card-content">
                    <div class="card-title">
                        {{ user.getAccountName }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col s12 l6">
            {% include(':User:listCharacters.html.twig') %}
        </div>
        <div class="col s12 l6">
            {% include(':User:listCredits.html.twig') %}
        </div>
    </div>

{% endblock %}
