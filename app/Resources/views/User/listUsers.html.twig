{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <h3>UserList:</h3>
        <div class="well-lg col s9 push-s1">
            <table class="bordered highlight responsive-table">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Tränen</th>
                        <th>Waffen</th>
                        <th>Accessoires</th>
                        <th>Urnen</th>
                        <th>Items</th>
                    </tr>
                </thead>
                <tbody>
                {% for user in users %}
                    <tr>
                        <td>{{ user.getAccountName }}</a></td>
                        <td>{{ user.getTear }}</td>
                        <td>{{ user.getWeapon }}</td>
                        <td>{{ user.getAcc }}</td>
                        <td>{{ user.getUrn }}</td>
                        <td>{{ user.getItem }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

{% endblock %}

